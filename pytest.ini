[pytest]
addopts = -q --cov --cov-report= --forked -n auto --ruff
required_plugins = pytest-forked pytest-xdist pytest-ruff pytest-cov
filterwarnings =
    ignore:.*may lead to deadlocks.*:DeprecationWarning
